import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    body: "Codec Regular, sans-serif",
    heading: "Codec Regular, sans-serif",
  },
});

export default theme;
